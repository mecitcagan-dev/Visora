GROQ_API_KEY=
# Optional for local CLI only. Production uses per-request BYOK from the UI.
# Comma-separated frontend origins (no trailing slash). Must include your Vercel URL.
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://visora-studio.vercel.app
